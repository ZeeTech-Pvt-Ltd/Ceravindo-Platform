# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.ssr-dist
*.local

# Temp verification scripts / scratch
.arttmp/

# Original images supplied by hand. The converted WebP that the site actually
# loads lives in public/ and is committed; these are the 6 MB sources kept for
# re-deriving, and they have no business in the repo or the deploy.
source-images/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
